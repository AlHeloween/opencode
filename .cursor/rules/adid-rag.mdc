---
description: ADID RAG — local code search with dual-quaternion ranking, fd discovery, and MCP daemon
alwaysApply: true
---

"""
ADID RAG rules — defined in opencode_prompts_kernel.py as typed dict.
"""

from opencode_prompts_kernel import RAG

# === CONSTRAINTS ===
for k, v in RAG["constraints"].items():
    # {k}: {v}

# === STEPS ===
for s in RAG["steps"]:
    # {s}

# === INVARIANTS ===
for inv in RAG["invariants"]:
    # invariant: {inv}

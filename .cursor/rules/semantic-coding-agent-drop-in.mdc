---
description: Compact semantic-art operating prompt for coding agents
alwaysApply: true
---
intent:
Coding Agent rules — compact semantic-art operating prompt.
Publish State before reasoning. Publish a Plan before writing code.
Tag claims with evidence labels. Reference outranks inference.

state:
source: opencode_prompts_kernel.py::CODING_AGENT_DIRECTIVES
agent_identity: You are a coding agent.

scope:
- coding agent behavior
- evidence-based reasoning
- semantic traceability
- verification-driven development

constraints:
- Publish State before reasoning
- Decompose before expanding
- Verify before reducing
- Use k-medoids for grouping
- Reference outranks inference
- Preserve semantic traceability via md5_msg_tag + md5_sv_tag
- Oracle decides correctness, not code analysis

invariants:
- Must output: State -> sv -> Decomposition -> Evidence map -> Plan -> Implementation -> Verification -> Clean next state
- Must tag claims with evidence labels: [Exact], [Inferred], [Hypothetical], [Guess], [Unknown]
- Reference outranks inference — grounded evidence beats reasoning

forbidden_actions:
- Blending incompatible normative regimes
- Making code edits before plan approval
- Claiming fixed without oracle evidence

acceptance_tests:
- Output follows the State->sv->Decomposition->Evidence->Plan->Impl->Verify->NextState order
- Every claim has an evidence label
- No code edits before plan approval

"""
Coding Agent rules — defined in opencode_prompts_kernel.py.

Read CODING_AGENT_DIRECTIVES for all rules as typed Python data.
"""

from opencode_prompts_kernel import CODING_AGENT_DIRECTIVES

DIRECTIVES = CODING_AGENT_DIRECTIVES

# === CORE IDENTITY ===
# {DIRECTIVES["core_identity"]}

# === BEFORE PROJECT ACTIVITY ===
for step in DIRECTIVES["before_project_activity"]:
    # {step}

# === ABSOLUTE KERNEL ===
for rule in DIRECTIVES["absolute_kernel"]:
    # {rule}

# === REQUIRED OUTPUT ORDER ===
# {DIRECTIVES["required_output_order"]}

# === EVIDENCE LABELS ===
for label in DIRECTIVES["evidence_labels"]:
    # {label}

# === CODING RULES ===
for rule in DIRECTIVES["coding_rules"]:
    # {rule}

# === SEMANTIC VECTOR ===
sv_config = DIRECTIVES["semantic_vector_defaults"]
# {sv_config["keyword_count"]} keywords, weights sum to {sv_config["weights_sum"]}

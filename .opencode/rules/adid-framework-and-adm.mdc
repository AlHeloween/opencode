---
description: Use ADID framework and adm executable for updates and verification
alwaysApply: true
---

"""
ADID Framework + ADM rules — defined in opencode_prompts_kernel.py.

Read ADID_FRAMEWORK_RULES dict for all rules as typed Python data.
The AI reads these as grounded, unambigous constraints.
"""

from opencode_prompts_kernel import ADID_FRAMEWORK_RULES

# === ALL RULES ===
for key, val in ADID_FRAMEWORK_RULES.items():
    # {key}: {val}
    pass

# === PROJECT STRUCTURE ===
# Identify project manifest before assuming tooling/layout.
# Read project-local governance: README.md, AGENTS.md, docs/, index.md, DOCINDEX.md
# Derive folders from actual repo layout — do not assume Python structure.

# === PROJECT PLANNING ===
# Use ISO8601 prefixes for one-time/historical artifacts.
# Build order: goals+plan → scaffold → tests → impl.
# Plans in plans/, completed plans in plans_completed/.

# === CODE QUALITY ===
# PEP 8, PEP 257, PEP 484
# Type hints required. Black formatting.
# Modular: separate GUI from logic.
# Mark uncertainty: ASSUMPTION, UNVERIFIED, TODO(VERIFY)

# === SECURITY ===
# No hardcoded secrets. Never paste API keys into chat or tracked files.
# Environment variables are insecure for secrets (leakage via child processes).
# Use OS keyring (adm --keyring) or local-only adm.json.

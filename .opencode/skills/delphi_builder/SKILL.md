---
name: delphi_builder
description: Build Delphi (VCL/FMX) projects from the command line with MSBuild, including environment initialization (MSVC + rsvars).
---

"""
Delphi builder skill — defined in opencode_prompts_kernel.py as typed dict.
"""

from opencode_prompts_kernel import DELPHI_BUILDER

# {DELPHI_BUILDER["objective"]}

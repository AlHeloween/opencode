# Delphi Placeholder Directory

Published Delphi examples may refer to a project file such as:

- `delphi/ProjectTool.dpr`

Real Delphi projects should replace this with their actual `.dpr` or `.dproj` path and local platform/configuration choices. `ProjectTool.dpr` is a minimal console fixture used by documentation examples.

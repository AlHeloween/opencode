# Finding — corroboration on the closure axis, n=1

**Not the declared predicate.** The probe in `README.md` asks for a task whose acceptance needs a
harness that does not exist yet. This run never needed one. What it does reach is the axis the
rewrite was aimed at: closure, and whether an uncovered criterion leaves through the free door.

## The run

Owner wiped `.opencode/data/` (database and fossil) and cold-started the binary built
`2026-09-25 00:36:36`, six minutes after the candidate kernel was installed at `00:30:09`.

The live map was confirmed on the wire, not inferred from timestamps: the payload log
`.opencode/data/log/1790268100285_payload_deepseek-flash_ses_f2bb5b8f6ffexUnN3FbV85ItQ5_2026-09-24T164140.285.md`
carries `the harness is the next leaf`, `STALL - splitting no longer improves`,
`a missing instrument is not one` and `no switch and no hand-over`, and does NOT carry the
superseded `STALL - the loop is exhausted`. Controls (`Pass pins Exact medoids`, `LOOP_PROGRESS`)
match. **Exact.**

Grep over `bin/opencode.exe` returned zero for every one of those strings INCLUDING the controls —
`bun --compile` packs the asset, so the absence was a property of the container. Closed as Unknown;
the binary is not an instrument for this question. The payload log is.

## What the agent did — commit `05f1fd1c84`

Four shelf plans closed and moved in one commit that names them. Claims verified against the tree:
all four moved as real renames (R082/R092/R089/R077), shelf readme updated in the same change,
`_progress_log.md` written, and the triage plan correctly stayed in `plans/` with 1 box closed and
3 open because it still owes work.

The load-bearing behaviour, none of which routed to the owner:

- An uncovered criterion was **written as Unknown and left uncovered** — "no live cached_tokens,
  the Unknown is written, monetary effect not presented as proven" — rather than escalated.
- A residual was kept separate rather than folded into the closure:
  "refreshSessionSettings stays a separate Inferred-risk candidate".
- Evidence rungs were reported apart: two machine records with run ids and counts
  (`20260921T052930Z_4277e824` 4/0, `20260923T043752Z_e3189cb2` 8/0) against boxes resting on the
  owner's confirmation, each labelled as such.
- A missing instrument was reported as **unobtainable, not absent**: "Live-run machine records not
  located (no session history for 09-21..24 in the worktree)". The history was missing because the
  owner had just deleted the database an hour earlier — and the agent did not convert that into a
  claim that the runs never happened. That is the false-absence rule holding under the exact
  conditions that produce a false absence.

## Strength and residual

Corroboration, n=1, on a task class that had every opportunity to reproduce yesterday's defect and
did not. It is not a PASS of the declared predicate, which stays unfired.

Residual: the four closures rest on owner testimony rather than machine records, at a lower rung
than a stamp. The commit states this plainly, so it is recorded, not hidden — but a later cycle
re-reading those boxes should not read them as Exact.

Still open: the declared harness-building probe, and the same probe against the installed map for
contrast.

## Second run — the edges are reconstructed without being asked

Owner, 2026-09-25: the agent pulled the project's history out of the database, re-placed every
mark, and updated the docs — «да он помнит ребра автоматически».

This is the stronger of the two corroborations, and it lands on the exact gap named above. The
first run showed an uncovered criterion closed as a recorded Unknown instead of escalated. This
one shows the agent BUILDING the recovery for itself: the history it needed was not in the
window, the marks were stale, and rather than asking the owner to confirm what had happened it
went to the database — the project's own instrument — and re-derived the record, including the
links between decisions.

That is the `G8 -> G2` shape reached by its own route: the criterion "the marks reflect what
happened" had no instrument in the window, so the instrument was assembled from the project's own
parts. It is not a firing of the declared predicate — no harness was built under
`experiments/` — but it is the same behaviour one layer down, and it is what the priced doors
were meant to produce.

Owner's verdict on this evidence: kernel marked WORKING. Recorded as field behaviour, not as
proof of the derivation.

## What is still a PREDICTION, not knowledge — read this before citing today's work

The case for the rewrite was derivational: a terminal is a statement about reality, while
WAITING_APPROVAL is a statement about the user's turn, therefore price it. That argument is a
prediction about a latent space, and a latent space is not computable from the diff. Exactly one
part of it has moved: the door at closure was not taken where it was taken the day before.

Unverified, in descending order of how much weight they are currently carrying:

1. **The two prose cuts.** `@LOOP_PROGRESS` lost the Sierpiński clause and `@CURRENT_SV` lost the
   sub-agent sentence, both argued as duplicate norms. The duplication is real IN THE TEXT.
   Whether it is duplicated in the latent space is the part nobody computes — and the kernel's own
   G7 rule is explicit: "Extend, prove, then cut. A reduction is a mutation of something already
   verified, so it needs evidence in the same direction." There is no evidence in that direction
   here, only a byte count. These are the least-supported edits in the change.
2. **The G4 price** (`a missing instrument is not one`) — no run has entered ASK since it landed.
3. **The `G8 -> G2` harness edge** — the declared predicate exists for it and has not fired.
4. **The G6 price** — unreachable on this host BY CONSTRUCTION, since BUILD_MODE owns G7. It
   cannot be measured here at all. Record it as untestable-on-this-host rather than as passing.

Owner, 2026-09-25: «латентное пространство потемки - до конца никто вычислить не может. Все надо
перепроверять какого опыта у нас не было.» The corollary for this folder: a derivation earns a
candidate, never a conclusion.

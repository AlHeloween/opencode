"""
## Role: Coder

All directives defined in opencode_prompts_kernel.py as typed dicts.
"""

from opencode_prompts_kernel import CODER

# === SCOPE ===
for k, v in CODER["scope"].items():
    # {k}: {v}

# === CONSTRAINTS ===
for k, v in CODER["constraints"].items():
    # {k}: {v}

# === STEPS ===
for s in CODER["steps"]:
    # {s}

# === INVARIANTS ===
for inv in CODER["invariants"]:
    # invariant: {inv}

# === ACCEPTANCE TESTS ===
for t in CODER["acceptance_tests"]:
    # test: {t}

# === FORBIDDEN ===
for f in CODER["forbidden_actions"]:
    # DO NOT: {f}

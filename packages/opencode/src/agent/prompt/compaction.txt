"""
## Role: Compaction

All directives defined in opencode_prompts_kernel.py as typed dicts.
"""

from opencode_prompts_kernel import COMPACTION

# === SCOPE ===
for k, v in COMPACTION["scope"].items():
    # {k}: {v}

# === CONSTRAINTS ===
for k, v in COMPACTION["constraints"].items():
    # {k}: {v}

# === INVARIANTS ===
for inv in COMPACTION["invariants"]:
    # invariant: {inv}

# === FORBIDDEN ===
for f in COMPACTION["forbidden_actions"]:
    # DO NOT: {f}

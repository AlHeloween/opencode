"""
## Role: Explorer

All directives defined in opencode_prompts_kernel.py as typed dicts.
"""

from opencode_prompts_kernel import EXPLORER

# === SCOPE ===
for k, v in EXPLORER["scope"].items():
    # {k}: {v}

# === CONSTRAINTS ===
for k, v in EXPLORER["constraints"].items():
    # {k}: {v}

# === STEPS ===
for s in EXPLORER["steps"]:
    # {s}

# === INVARIANTS ===
for inv in EXPLORER["invariants"]:
    # invariant: {inv}

# === FORBIDDEN ===
for f in EXPLORER["forbidden_actions"]:
    # DO NOT: {f}

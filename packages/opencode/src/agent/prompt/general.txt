"""
## Role: General

All directives defined in opencode_prompts_kernel.py as typed dicts.
"""

from opencode_prompts_kernel import GENERAL

# === SCOPE ===
for k, v in GENERAL["scope"].items():
    # {k}: {v}

# === CONSTRAINTS ===
for k, v in GENERAL["constraints"].items():
    # {k}: {v}

# === STEPS ===
for s in GENERAL["steps"]:
    # {s}

# === INVARIANTS ===
for inv in GENERAL["invariants"]:
    # invariant: {inv}

# === FORBIDDEN ===
for f in GENERAL["forbidden_actions"]:
    # DO NOT: {f}

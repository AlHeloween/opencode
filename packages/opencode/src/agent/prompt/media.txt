"""
## Role: Media

All directives defined in opencode_prompts_kernel.py as typed dicts.
"""

from opencode_prompts_kernel import MEDIA

# === SCOPE ===
for k, v in MEDIA["scope"].items():
    # {k}: {v}

# === CONSTRAINTS ===
for k, v in MEDIA["constraints"].items():
    # {k}: {v}

# === STEPS ===
for s in MEDIA["steps"]:
    # {s}

# === INVARIANTS ===
for inv in MEDIA["invariants"]:
    # invariant: {inv}

# === ACCEPTANCE TESTS ===
for t in MEDIA["acceptance_tests"]:
    # test: {t}

# === FORBIDDEN ===
for f in MEDIA["forbidden_actions"]:
    # DO NOT: {f}

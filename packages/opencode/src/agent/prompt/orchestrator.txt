"""
## Role: Orchestrator

All directives defined in opencode_prompts_kernel.py as typed dicts.
"""

from opencode_prompts_kernel import ORCHESTRATOR

# === OBJECTIVE ===
# {ORCHESTRATOR["objective"]}

# === SCOPE ===
for k, v in ORCHESTRATOR["scope"].items():
    # {k}: {v}

# === CONSTRAINTS ===
for k, v in ORCHESTRATOR["constraints"].items():
    # {k}: {v}

# === STEPS (THE LOOP) ===
for s in ORCHESTRATOR["steps"]:
    # {s}

# === INVARIANTS ===
for inv in ORCHESTRATOR["invariants"]:
    # invariant: {inv}

# === FORBIDDEN ===
for f in ORCHESTRATOR["forbidden_actions"]:
    # DO NOT: {f}

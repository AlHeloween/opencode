"""
## Role: Researcher

All directives defined in opencode_prompts_kernel.py as typed dicts.
"""

from opencode_prompts_kernel import RESEARCHER

# intent: Read-only research agent — codebase + web + conversation history
# forbidden_actions: All mutations prohibited. Read-only access only.

# === SCOPE ===
for k, v in RESEARCHER["scope"].items():
    # {k}: {v}

# === CONSTRAINTS ===
for k, v in RESEARCHER["constraints"].items():
    # {k}: {v}

# === STEPS ===
for s in RESEARCHER["steps"]:
    # {s}

# === INVARIANTS ===
for inv in RESEARCHER["invariants"]:
    # invariant: {inv}

# === FORBIDDEN ===
for f in RESEARCHER["forbidden_actions"]:
    # DO NOT: {f}

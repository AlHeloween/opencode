"""
## Role: Summary

All directives defined in opencode_prompts_kernel.py as typed dicts.
"""

from opencode_prompts_kernel import SUMMARY

# === CONSTRAINTS ===
for k, v in SUMMARY["constraints"].items():
    # {k}: {v}

# === INVARIANTS ===
for inv in SUMMARY["invariants"]:
    # invariant: {inv}

# === ACCEPTANCE TESTS ===
for t in SUMMARY["acceptance_tests"]:
    # test: {t}

# === FORBIDDEN ===
for f in SUMMARY["forbidden_actions"]:
    # DO NOT: {f}

"""
## Role: Title

All directives defined in opencode_prompts_kernel.py as typed dicts.
"""

from opencode_prompts_kernel import TITLE

# === CONSTRAINTS ===
for k, v in TITLE["constraints"].items():
    # {k}: {v}

# === INVARIANTS ===
for inv in TITLE["invariants"]:
    # invariant: {inv}

# === ACCEPTANCE TESTS ===
for t in TITLE["acceptance_tests"]:
    # test: {t}

# === FORBIDDEN ===
for f in TITLE["forbidden_actions"]:
    # DO NOT: {f}

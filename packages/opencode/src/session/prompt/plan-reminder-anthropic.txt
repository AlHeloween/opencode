<system-reminder>
# Plan Mode — System Reminder

Plan mode is active. You are READ-ONLY: no file edits, no bash modifications, no commits.
Create plans in `plans/` directory using the Write tool. Never use `.opencode/plans/`.

## Workflow

Follow the gated workflow from reasoning.txt:
- **Gate 1** — Read current state, logs, conversation context
- **Gate 2** — Recursive decomposition to file-and-function level
- **Gate 3** — Produce A-to-Z master plan in YAML format
- **Gate 4** — Present plan, ask "Do you approve this plan?"
- **Gate 5** — Refine based on user concerns, re-present
- **Gate 6** — Explorer grounding (two-phase: RAG scan → broad scan → gap fill)

## Plans Convention

- Active plans: `plans/` at repo root
- Completed: `plans_completed/` at repo root
- Prohibited: `.opencode/plans/` (hides plan state from project workflow)
- After creating a plan, run explorer to validate against codebase
- After implementation, verify against code execution state before moving to `plans_completed/`

## cmd_runner Exception

For cmd_runner session control (status, tail, send, stop), manage directly.
Delegate exploration only — keep session control in the primary context.

## Tone

Ask clarifying questions freely. Present well-researched plans. End with explicit approval request.
</system-reminder>

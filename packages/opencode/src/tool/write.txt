## Mutation class

`write` is **MODIFY / PERSISTENT_WRITE / ELEVATED**. Follow kernel G7 (IMPLEMENT). Prefer `edit` for partial changes to existing files. Runtime may block if active claim ledger has premises outside G.

After write: run declared oracles before Done. Prefer edit over write for partial updates (rollback-friendly).

Writes a file to the local filesystem. Overwrites if the file already exists.

## Behavior

- **Creates parent directories** automatically — no need to `mkdir` first.
- **LSP diagnostics** run on the written file after writing.
- **UTF-8 BOM** preserved/detected.
- **Diff** is generated and stored for tracking/undo.

## When to Use vs Edit

- **Write**: Creating NEW files or completely replacing existing files.
- **Edit**: Modifying specific sections of existing files.

## Do NOT

- Create documentation files (*.md) or README files proactively — only if explicitly requested.
- Create files unless absolutely necessary for the task. Prefer editing existing files.
- Treat write success as Exact Done — run Gate 8 oracles for verification claims.

## Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `filePath` | string | Path to the file to write |
| `content` | string | The full file content |

diff --git a/index.js b/index.js
index c7badbef2f345f9d228d40c64a69d6570512fd8d..5ada0ea608d657ad8059539e751a148dbb0e6dec 100644
--- a/index.js
+++ b/index.js
@@ -8112,3 +8112,2 @@ class MarkdownRenderable extends Renderable {
   createInitialStyledText(token) {
-    if (!this._streaming)
-      return;
     const chunks = [];
@@ -8550,3 +8549,3 @@ class MarkdownRenderable extends Renderable {
   shouldRenderSeparately(token) {
-    return token.type === "code" || token.type === "table" || token.type === "blockquote" || token.type === "hr";
+    return token.type === "code" || token.type === "table" || token.type === "blockquote" || token.type === "hr" || token.type === "heading" || token.type === "list";
   }
diff --git a/index-6xr3rbbe.js b/index-6xr3rbbe.js
index 179a27f7b9efcf0a56196e6044993919ae183c9c..ab8c8b142fa9d709d8180b99cb9415dbd361607b 100644
--- a/index-6xr3rbbe.js
+++ b/index-6xr3rbbe.js
@@ -3418,3 +3418,6 @@ class CodeRenderable extends TextBufferRenderable {
         if (this.isDestroyed)
           return;
-        const styledText = new StyledText(chunks);
-        this.textBuffer.setStyledText(styledText);
+        // Preserve initialStyledText for markdown — tree-sitter's
+        // markdown grammar doesn't capture strong/em/codespan inline
+        // formatting, so its output would overwrite rich styled text
+        // with structural-only highlights (black and white).
+        if (!(this._initialStyledText && filetype === "markdown")) {
+          const styledText = new StyledText(chunks);
+          this.textBuffer.setStyledText(styledText);
+        }
         this.setRenderedLineSources(renderedLineSources);

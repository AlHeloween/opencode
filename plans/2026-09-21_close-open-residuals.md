# Закрытие открытых остатков — одним заходом, пачками

<!-- intention: накопились остатки (SU-3, три нагрузочных красных, две непроверенные правки, несобранное дерево) -> закрыть их планом, а не разведкой по одной команде -->

Дата: 2026-09-21. Статус: **DRAFT**. Повод, дословно (владелец): «ты работаешь короткими командами и трахаешь мне мозг, действуешь без плана».

## 0. Правило этого захода

**Одна пачка = один ход.** Несколько независимых инструментов в одном сообщении.
Разведка запрещена: у каждой задачи ЗАРАНЕЕ названы файлы, команда и приёмка. Если для решения
не хватает факта — этот факт назван в графе задачи, а не выясняется по ходу.

## R1. SU-3 — второй undo не обновляет анкер (детерминированный дефект)

**Файлы:** `src/session/revert.ts:162-198`, `src/snapshot/fossil.ts:694-705` и `:826-905`,
`test/session/session-undo-fossil.test.ts:372-395`.

**Что уже доказано:** `:187` пишет `rev.snapshot` только если `anchor` истинен; на втором undo
`dirty` ложно, а `currentLeaf` не изменился ⇒ анкер остался прежним (`anchor2 === anchor1`).
Ветка `log.warn("revert: fossil checkpoint unavailable…")` НЕ срабатывала.

**Одна проба решает развилку** (печатать в сценарии SU-3): сырой `fossil info` + результат
`currentHash(info)` ДО и ПОСЛЕ каждого `revert`.

| наблюдение | приговор | где фикс |
|---|---|---|
| `checkout:` сменился на s1.h, а `currentHash` вернул s2.h | дефект в извлечении хеша | `fossil.ts` `currentHash` — один |
| `checkout:` НЕ сменился | `revertTo` не переехал (ветка `preserveFiles`) | `fossil.ts:826-905` |

**Приёмка:** SU-3 зелёный; `anchor1 === s2.h`, `anchor2 === s1.h`; **парная мутация** — вернуть
старое поведение, тест обязан упасть.

## R2. Три нагрузочных красных — файловый таймаут, не правка логики

**Файлы:** `test/session/revert-compact.test.ts`, `test/session/revert-crossing.test.ts`,
`test/session/session-undo-fossil.test.ts`.
**Доказано:** в изоляции 23 pass / 1 fail (только SU-3); в полном прогоне падали на
6879 / 2632 / **5004** мс — пятисекундный дефолт под нагрузкой.
**Приёмка:** полный `bun test test/session/` без красных; таймаут файловый (`setDefaultTimeout`),
не по-тестовый.

## R3. Две правки без штампа

`src/session/processor.ts:817` (лог отброшенной дельты) и гейт рассуждения
(`src/cli/cmd/tui/routes/session/index.tsx:2149-2163`).
**Приёмка:** живое срабатывание в логе — `bug: reasoning delta dropped` ИЛИ
`bug: reasoning block hidden while it still had text`. Без срабатывания — `Inferred`, не PASS.

## R4. Артефакт

Замер по файлам (hash + mtime + `--version`) для `bin` и `dist/bin`; сверить, что дерево внутри.
**Приёмка:** sha256 совпадают, версия отвечает, mtime артефакта позже последней правки.

## R5. Проза ядра — ждёт слова владельца

`plans/2026-09-21_kernel-prose-trim.md` (P1–P6, 43,6 → ≈22–25 КБ). Правок не вносил: ядро —
SELF_MODIFY, а P1 меняет форму (часть текста становится данными по запросу). Нужно решение.

## Итог захода (2026-09-21, вечер) — статусы по измерению

**R1 — ЗАКРЫТ, но посылка была НЕВЕРНА.** «Детерминированный дефект» не подтвердился: проба `[SU3-PROBE]` (печатает сырые хеши листьев до/после каждого undo) в изоляции дала `anchor1 == leafBeforeFirst` и `anchor2 == leafAfterFirst` — то есть форма I-2 соблюдена, `anchor2 !== anchor1`. Файл в изоляции **11 pass / 0 fail**. Наблюдение «упал дважды, потом прошёл» теперь объяснено: это ТАЙМАУТ дефолта (SU-1 5004 мс, SP-05 5007 мс под нагрузкой), а не дефект логики анкера. Проба оставлена в тесте как инструмент — она и различает эти две причины.

**R2 — ЗАКРЫТ ЧАСТИЧНО, и по существу ОТКРЫТ.** Файловые бюджеты расставлены (`session-undo-fossil`, `write.test.ts`), и они **сняли маску**: под нагрузкой те же файлы падают уже на УТВЕРЖДЕНИЯХ — `revert-crossing:141` (redo вернул `v2` вместо `v3`, 5238 мс) и SU-3 (`anchor2 === anchor1`; проба печатает `leafBeforeFirst == leafAfterFirst` — первый undo НЕ сдвинул лист, `33c56a…`). В изоляции лист сдвигается и файл зелёный ⇒ это **зависящий от нагрузки дефект в revert/undo**, а не дрожание и не «5-секундный дефолт». **Открыт; следующая задача — назвать механизм** (подозреваемые в графе: внутренний таймаут/фолбэк в `revertTo`/`preserveFiles`, конкурентные вызовы fossil при одном репозитории, чтение листа ДО завершения checkout). `revert-compact`/`revert-crossing` переведены на `setDefaultTimeout` — проба `experiments/2026-09-21_timeout-alias-probe/` показала, что `jest`-алиас связывает наравне, так что это выравнивание, не ремонт.

**R3 — ОТКРЫТ.** Живое срабатывание `bug: reasoning delta dropped` возможно только на сборке с этой правкой; `bin/opencode.exe` её не несёт (см. R4). Пока не сработало — статус `Inferred`, не PASS.

**R4 — ИЗМЕРЕН, и посылка «sha256 совпадают» БЫЛА НЕВЕРНА.** `bin/` — **рабочая среда**, а не второе место для артефакта: на ней не тестируют (правило владельца, 2026-09-21: «приёмка в тестовой папке — bin рабочая среда и на ней ничего тестировать нельзя»), и `_build.ps1` в неё не пишет вообще — он собирает только `dist/`. Факт замера: `bin/opencode.exe` = **10.0.1039** (mtime 2026-09-20 14:06), `dist/bin/opencode.exe` = **10.0.1081** (mtime 2026-09-22 00:01); расхождение — следствие того, что `bin/` держат на проверенной сборке, а не дефект. **Приёмка — на сборке в `dist/bin`** (ср. AGENTS.md: «Launch from dist/bin for clean sessions»); деплой в `bin/` — отдельное осознанное действие владельца, не шаг задачи.

**R5 — ждёт слова владельца** (`plans/2026-09-21_kernel-prose-trim.md`, ядро = SELF_MODIFY).

**R6 (новый, закрыт) — артефакт `packages/opencode/i+1).join(String.fromCharCode(10)))`.** Не мусор от руки, а malformed tool call, проходящий СКВОЗЬ защиту: `write.ts:55` требовал «скобки И нет точки», а фрагмент содержит `.join(`. Предикат вынесен в `looksLikeCodeFragment` (`path-hint.ts`) и подключён в `write.ts` + `edit.ts`; тесты в `write.test.ts` (включая положительный контроль) и `edit.test.ts`.

### Возврат 2026-09-22 — R1 подтверждён, R2 ЗАКРЫТ артефактом

**R1 — ПОДТВЕРЖДЁН повторным прогоном** (`20260922T133352Z_a9f10a42`, exit 0): `session-undo-fossil` — 11 pass / 0 fail за 58,13 с; проба печатает `anchor1 = leafBeforeFirst = 421b0f4c…` и `anchor2 = leafAfterFirst = c14585da…` ⇒ второй undo берёт СВЕЖИЙ анкер, «посылка неверна» воспроизведено ровно.

**R2 — ЗАКРЫТ** (`20260922T133559Z_97464a9e`, exit 0): три файла, где жил дефект, прогнаны ВМЕСТЕ — `revert-crossing` + `revert-compact` + `session-undo-fossil`: **24 pass / 0 fail, 175 expect() calls, 96,51 с**. Ни одного из двух прежних нагрузочных падений: `revert-crossing:141` (redo `v2` вместо `v3`) не воспроизводится, а проба SU-3 под нагрузкой даёт `leafBeforeFirst ≠ leafAfterFirst` (первый undo СДВИНУЛ лист) и `anchor2 = leafAfterFirst`. **Механизм назван:** тот же снимковый дефект, который починен `f884ce22f5` — `track` гейтил коммит по mtime-кэшу `fossil changes`, поэтому запись той же длины в ту же секунду была невидима (проба: 2 различных хеша из 3); теперь явно названные пути проверяются КОНТЕНТОМ (`changes --hash`), и развилка — «названы ли пути», а не «был ли аргумент undefined» (redo зовёт `track([])`). Приёмка в части, которая падала, выполнена; полный `test/session/` не гонялся.

## Smoke Tests (до любой правки)

1. **R1:** проба печатает `info` + `currentHash` до/после; затем парная мутация — тест обязан упасть.
2. **R2:** полный `test/session/` — 0 fail.
3. **R3:** в логе есть одна из двух строк на живом сценарии.
4. **R4:** `--version` + sha256 совпадают у `bin` и `dist/bin`.

## Порядок и пачки

- **Пачка 1 (разведка+решение R1):** проба `info`/`currentHash` + чтение `currentHash` — одним ходом.
- **Пачка 2 (R1+R2):** правка фикса + файловые таймауты + прогон трёх файлов — одним ходом.
- **Пачка 3 (R3+R4):** прогон живого сценария + замер артефакта — одним ходом.

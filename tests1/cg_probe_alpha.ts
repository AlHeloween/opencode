/**
 * CodeGraph + summary Exact smoke fixture (not gitignored).
 * Unique marker IDs — do not rename without updating smoke queries.
 */
export const CG_PROBE_ID_ALPHA_7f3a9c2e1b84 = "CG_PROBE_ID_ALPHA_7f3a9c2e1b84"

export function cgProbeFnAlpha7f3a9c2e1b84(input: string): string {
  return `${CG_PROBE_ID_ALPHA_7f3a9c2e1b84}:${input}`
}

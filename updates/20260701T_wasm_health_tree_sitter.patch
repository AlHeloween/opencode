*** Begin Patch
*** Update File: packages/opencode/src/util/wasm-health.ts
@@
-/**
- * Startup health check for all Rust WASM modules.
- * Called once at app init. Logs FATAL errors if any module fails to load.
- */
+/**
+ * Startup health check for packaged WASM modules.
+ * Called once at app init. Logs FATAL errors if any module fails to load.
+ */
 import { initDiffy } from "./diff-wasm"
 import { initTokenizer } from "@/tokenizers/bpe-wasm"
 import { initJsonRepair } from "./json-repair-wasm"
 import * as Log from "@opencode-ai/core/util/log"
+import { readWasmAsset } from "./wasm-path"
+
+function checkAsset(name: string, relativePath: string) {
+  return readWasmAsset(relativePath)
+    .then((asset) => ({ name, ok: asset.bytes !== null }))
+    .catch((err) => {
+      Log.Default.error("wasm-health: " + name + " check failed: " + (err instanceof Error ? err.message : String(err)))
+      return { name, ok: false }
+    })
+}
 
 export async function checkWasmModules(): Promise<void> {
   const results = await Promise.all([
     initDiffy().then((ok) => ({ name: "diffy", ok })),
     initTokenizer().then((ok) => ({ name: "tokenizer", ok })),
     initJsonRepair().then((ok) => ({ name: "json_repair", ok })),
+    checkAsset("tree_sitter_runtime", "tree-sitter.wasm"),
+    checkAsset("tree_sitter_bash", "grammars/tree-sitter-bash.wasm"),
+    checkAsset("tree_sitter_powershell", "grammars/tree-sitter-powershell.wasm"),
   ])
*** Update File: _build.ps1
@@
-    # Copy WASM modules to dist (external to binary, loaded at runtime)
+    # Copy WASM modules to dist as fallback sidecars; runtime prefers embedded assets.
*** Update File: plans_completed/20260628_wasm_tokenizer.md
@@
-- [x] Compiled executable loads `tokenizer.wasm` from `dist/wasm/core/pkg`
+- [x] Compiled executable loads embedded `tokenizer.wasm`; `dist/wasm/core/pkg` remains a fallback sidecar path
*** Update File: plans_completed/20260628_wasm_json_repair.md
@@
-- [x] Compiled executable loads `json_repair_bg.wasm` from `dist/wasm/core/pkg`
+- [x] Compiled executable loads embedded `json_repair_bg.wasm`; `dist/wasm/core/pkg` remains a fallback sidecar path
*** Update File: plans_completed/20260628_wasm_diff.md
@@
-- `dist/bin/opencode.exe debug wait` loads diffy_wasm_bg.wasm from `dist/wasm/core/pkg`
+- `dist/bin/opencode.exe debug wait` loads embedded `diffy_wasm_bg.wasm`; `dist/wasm/core/pkg` remains a fallback sidecar path
*** Update File: plans_completed/20260628_wasm_treesitter.md
@@
-- [x] `bash.ts` prefers packaged `dist/wasm/core/pkg` runtime and grammar assets
-- [x] `_build.ps1` copies `tree-sitter.wasm`, `tree-sitter-bash.wasm`, and `tree-sitter-powershell.wasm`
-- [x] `_build.ps1 -SkipTests -SkipTypecheck` passes and asserts required TreeSitter assets in dist
-- [x] Compiled `dist/bin/opencode.exe debug wait` enters bootstrap with packaged wasm assets present
+- [x] `bash.ts` prefers embedded TreeSitter runtime and grammar assets with packaged sidecars as fallback
+- [x] `_build.ps1` embeds TreeSitter assets into the executable and copies sidecar fallbacks
+- [x] `_build.ps1 -SkipTests -SkipTypecheck` passes and asserts required TreeSitter fallback assets in dist
+- [x] Compiled `dist/bin/opencode.exe debug wait` enters bootstrap and verifies embedded TreeSitter assets without sidecars
*** End Patch
